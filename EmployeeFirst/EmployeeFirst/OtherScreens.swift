//
//  OtherScreens.swift
//  EmployeeFirst
//
//  Created by praveen on 16/12/24.
//

import SwiftUI


struct AchievementsView: View {
    var body: some View {
        Text("Achievements")
            .font(.largeTitle)
            .padding()
    }
}

struct ProjectsView: View {
    var body: some View {
        Text("Projects Completed")
            .font(.largeTitle)
            .padding()
    }
}

struct TimeAttendanceView: View {
    var body: some View {
        Text("Time and Attendance")
            .font(.largeTitle)
            .padding()
    }
}

struct OrgStructureView: View {
    var body: some View {
        Text("Org Structure")
            .font(.largeTitle)
            .padding()
    }
}

struct SalaryDetailsView: View {
    var body: some View {
        Text("Salary Details")
            .font(.largeTitle)
            .padding()
    }
}

struct LeaveApplicationView: View {
    var body: some View {
        Text("Leave Application")
            .font(.largeTitle)
            .padding()
    }
}

struct SettingsView: View {
    var body: some View {
        Text("Settings")
            .font(.largeTitle)
            .padding()
    }
}

#Preview {
    AchievementsView()
}
